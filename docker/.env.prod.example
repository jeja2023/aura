# 生产环境变量模板（示例，占位值必须替换）

# 可选：由 API 容器托管静态前端时设为 /app/frontend，并在编排中挂载该目录；若仅由 Nginx/CDN 提供静态资源可留空或不设置
# PATHS__FRONTENDROOT=/app/frontend

# 基础镜像（建议改为企业私有仓库镜像）
POSTGRES_IMAGE=postgres:16-alpine
REDIS_IMAGE=redis:7-alpine
PYTHON_BASE_IMAGE=python:3.11-slim
# 构建镜像时使用；与 global.json 中 sdk.version 对齐
DOTNET_SDK_IMAGE=mcr.microsoft.com/dotnet/sdk:10.0.201
DOTNET_ASPNET_IMAGE=mcr.microsoft.com/dotnet/aspnet:10.0.201
API_IMAGE=your-registry/aura-api:REPLACE_TAG
AI_IMAGE=your-registry/aura-ai:REPLACE_TAG

# 基础连接
CONNECTIONSTRINGS__PGSQL=Host=pgsql;Port=5432;Database=aura;Username=prod_user;Password=REPLACE_ME
CONNECTIONSTRINGS__REDIS=redis:6379,password=REPLACE_ME
AI__BASEURL=http://ai:8000

# 安全密钥
JWT__KEY=REPLACE_WITH_LONG_RANDOM_JWT_KEY
SECURITY__HMACSECRET=REPLACE_WITH_LONG_RANDOM_HMAC_SECRET

# 告警通知
OPS__ALERT__WEBHOOKURL=
OPS__ALERT__FILEPATH=/app/storage/logs/alert-notify.log
OPS__METRICS__EXPOSEPROMETHEUS=false
OPS__TELEMETRY__ENABLETRACING=false
OPS__TELEMETRY__OTLPENDPOINT=
OPS__TELEMETRY__OTLPPROTOCOL=Grpc

# 巡检账号（仅用于自动化巡检脚本）
AURA_ADMIN_USER=admin
AURA_ADMIN_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

# AI 运行配置
ARANGO_URI=http://arangodb:8529
ARANGO_DB=aura
ARANGO_USER=_system
ARANGO_ROOT_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
ARANGO_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
AURA_AI_REQUIRE_ARANGO=true
AURA_MODEL_PATH=/models/osnet_ibn_x1_0.onnx
